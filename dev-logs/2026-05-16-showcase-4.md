# Showcase-4 Demo Polish and Final Verification

Date: 2026-05-16

## Scope

Finalize the Showcase phase by tightening demo-facing empty states, fixing a candidate selection edge case, adding final summary documentation, and verifying the product can build and run locally.

## UI / Product Polish

- `POISummaryPanel`
  - Replaced the ambiguous all-skeleton state for zero POI results with an explicit empty state.
  - The category summary grid now remains visible even when all enabled POI categories return no results.

- `CandidatePanel`
  - Clarified that candidates are stored in the current browser.
  - Clarified that add/delete refreshes the list immediately.
  - Improved the empty state to explain that 2+ candidates enable comparison.

- `CandidateComparisonPanel`
  - Clarified that preference score only affects comparison ranking.
  - Kept the original comprehensive score visible as the baseline score.

- `src/app/page.tsx`
  - Fixed candidate selection behavior when a candidate has no address B.
  - Selecting a candidate without B now clears the previous B address and stale commute result.
  - Selecting a candidate with B clears stale commute result and starts route recalculation.

## Documentation

- Added `docs/showcase-final-summary.md`.
- Updated README and version story to mark Showcase as finalized.

## Verification

- `npm run build` passed with Next.js 16.2.6.
- Local production server was started with `next start` on `127.0.0.1:3100`.
- HTTP checks passed:
  - `/` returned `200` and included Next.js app markup.
  - `/api/auth/session` returned `200` with `null` session.
  - `/api/route/driving` without params returned `400` with `{"error":"缺少参数"}`.
- Port `3100` was clear after verification.

## Expected Tag

Verification passed. Create tag `v1.3.0`.
