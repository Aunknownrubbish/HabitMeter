# 开发执行计划 — HabitMeter（寻栖）

## 总览

采用**渐进式交付**策略，每阶段产出可独立验证的功能增量，确保项目稳步推进。

---

## 第 1 阶段：项目骨架

**目标**：搭建 Next.js 项目、配置基础工具链、实现静态布局框架

### 任务清单
- [ ] `create-next-app` 初始化项目（TypeScript + App Router）
- [ ] 安装依赖：`tailwindcss`、`lucide-react`
- [ ] 配置 `tailwind.config.ts`（颜色、字体的自定义扩展）
- [ ] 创建基础目录结构
- [ ] 实现桌面端布局框架（左侧栏 400px + 右侧地图占位区）
- [ ] 实现移动端响应式布局（地图上 + 面板下）
- [ ] 创建基础 UI 组件（Card、Input、Button、Toggle）

### 完成标准
- `npm run dev` 正常启动
- 桌面端和移动端布局均可正常渲染
- 所有组件无 TypeScript 报错

---

## 第 2 阶段：地图 + 搜索

**目标**：集成高德地图、实现地址搜索和地图标注

### 任务清单
- [ ] 加载高德地图 JS API（`@amap/amap-jsapi-loader`）
- [ ] 创建地图组件，渲染全屏地图
- [ ] 实现地址 A 输入框 + 自动补全（AMap.AutoComplete）
- [ ] 实现地址 B 输入框 + 自动补全
- [ ] 地址 A 确定后：地图中心跳转、放置 Marker、绘制 3km 圆
- [ ] 地址 B 确定后：放置 Marker

### 完成标准
- 输入地址可自动补全
- 选择地址后地图正确跳转并标记
- 3km 半透明圆正确渲染

---

## 第 3 阶段：POI 搜索 + 通勤分析

**目标**：实现周边 POI 搜索、统计展示、通勤路线计算

### 任务清单
- [ ] 实现 POI 多类搜索（AMap.PlaceSearch，5 类 POI）
- [ ] POI 标记点在地图上渲染（按类别分色）
- [ ] 统计面板：各类 POI 数量卡片
- [ ] POI 详细列表：可展开，显示名称+距离
- [ ] POI 类别勾选开关（控制地图显隐 + 统计更新）
- [ ] A→B 路径规划（AMap.DrivingRoute + AMap.TransferRoute）
- [ ] 通勤面板：公交方案 + 驾车方案展示

### 完成标准
- 5 类 POI 可独立搜索和显示
- 统计面板数据准确
- 通勤路线渲染正常

---

## 第 4 阶段：数据库 + 认证 + 收藏

**目标**：实现用户登录和数据持久化

### 任务清单
- [ ] 初始化 Prisma + SQLite
- [ ] 定义数据模型（User、SavedLocation）
- [ ] 配置 NextAuth.js（邮箱密码登录）
- [ ] 实现收藏 CRUD API（`/api/locations`）
- [ ] 登录/注册 UI（模态框或侧边栏内嵌）
- [ ] 收藏列表 + 切换查看功能

### 完成标准
- 用户可注册、登录、登出
- 登录后可收藏地址并持久化
- 刷新页面后收藏数据不丢失

---

## 第 5 阶段：移动端适配 + 体验优化

**目标**：完善移动端体验、性能优化、边界情况处理

### 任务清单
- [ ] 移动端底部抽屉组件（可拖拽调整高度）
- [ ] 移动端触摸交互优化（地图手势、列表滚动）
- [ ] 加载状态与骨架屏
- [ ] 错误状态处理（搜索无结果、网络错误等）
- [ ] 边界情况处理（A 和 B 同城/跨城、POI 为空等）
- [ ] 性能优化（POI 搜索防抖、地图标记聚合）

### 完成标准
- 移动端交互流畅
- 各种边界情况有合理的 UI 反馈
- 无明显性能问题
